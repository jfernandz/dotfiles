#
# User configuration sourced by login shells
#

# Initialize zim (I don't need this anymore because
# it is system-wide installed)
#[[ -s ${ZIM_HOME}/login_init.zsh ]] && source ${ZIM_HOME}/login_init.zsh


